/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'i.pravatar.cc', 'ui-avatars.com', 'via.placeholder.com'],
  },
};

module.exports = nextConfig;